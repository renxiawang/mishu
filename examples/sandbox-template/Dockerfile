# Manual-test template for MISHU_SANDBOX_DOCKERFILE.
FROM docker/sandbox-templates:codex

ENV MISHU_TEMPLATE_TEST=local-dockerfile

RUN printf '%s\n' "Mishu local Dockerfile template loaded" > /tmp/mishu-template-marker.txt
